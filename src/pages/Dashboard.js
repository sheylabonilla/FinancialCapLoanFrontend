import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <h2>Bienvenido Necesitas Implmentar el Dashboard</h2>
        </div>
    );
};

export default React.memo(Dashboard);